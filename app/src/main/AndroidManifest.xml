<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.nibali.constraint_examples">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/mai"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/mai"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".Application"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name="com.vk.sdk.VKServiceActivity"
            android:label="ServiceActivity"
            android:theme="@style/VK.Transparent" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProfileActivity" />
        <activity android:name=".PostsActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".NavigationProfile"
            android:label="@string/title_activity_navigation_profile"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".UsersActivity" />
    </application>

</manifest>